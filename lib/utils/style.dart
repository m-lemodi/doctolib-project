

import 'package:flutter/material.dart';

BoxDecoration iconButton = BoxDecoration(
  color: Colors.green,
  borderRadius: BorderRadius.circular(10.0),
);

TextStyle TitleStyle = const TextStyle(
  color: Colors.green, // Couleur du texte définie par l'utilisateur
  fontSize: 22, // Taille du texte définie par l'utilisateur
  fontWeight: FontWeight.bold,
);

BoxDecoration box = BoxDecoration(
    border: Border.all(
      color: Colors.grey,
      width: 2.0,
  ),
  borderRadius: BorderRadius.circular(8.0),
);

